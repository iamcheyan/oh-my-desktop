#!/bin/sh
set -eu

# Store clipboard content and notify Quickshell to refresh
wl-paste --watch sh -c '
    cliphist store
    qs -p "$HOME/.config/quickshell" ipc call cliphistService update 2>/dev/null || true
'