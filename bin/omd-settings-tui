#!/usr/bin/env bash
# Route settings TUI pages — only routes with core binaries are active.
set -euo pipefail

OMD_ROOT="${OMD_ROOT:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}"
export OMD_ROOT

route="${1:-help}"

case "$route" in
  -h|--help|help)
    cat <<'EOF'
Usage: omd-settings-tui [page]

Pages:
  theme      Theme picker and active theme info
EOF
    exit 0
    ;;
  theme|appearance)
    exec python3 "$OMD_ROOT/quickshell/modules/settings/bin/omd-settings-theme-tui"
    ;;
  *)
    echo "omd-settings-tui: unknown page '$route' (only 'theme' available in core)" >&2
    exit 2
    ;;
esac
