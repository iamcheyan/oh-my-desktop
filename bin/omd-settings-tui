#!/usr/bin/env bash
set -euo pipefail

OMD_ROOT="${OMD_ROOT:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}"
export OMD_ROOT

APP="$OMD_ROOT/tui-go/omd-settings-tui"
"$OMD_ROOT/scripts/build-go-tools" --quiet

exec "$APP" "$@"
