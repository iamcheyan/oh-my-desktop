#!/usr/bin/env bash
set -euo pipefail
config_file="${SUMIKA_SHELL_CONFIG_HOME:-$HOME/.config/sumika-shell}/sumika.json"

mkdir -p "$(dirname "$config_file")"
if [[ ! -f "$config_file" ]]; then
  echo "{}" >"$config_file"
fi

exec xdg-terminal-exec --app-id=org.omd.config-edit vi "$config_file"
