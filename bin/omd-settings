#!/bin/sh
set -eu

# Thin shim: delegates to the external settings module's canonical implementation.
repo_root="$(cd "$(dirname "$0")/.." && pwd)"
export OMD_REPO_ROOT="$repo_root"
exec "${SUMIKA_MODULES_HOME:-$HOME/development/sumika-modules}/settings/bin/omd-settings" "$@"
