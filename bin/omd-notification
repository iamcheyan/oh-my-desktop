#!/bin/sh
set -eu

# Resolve repo root from this script's location
_omd_root=$(cd "$(dirname "$0")/.." && pwd)
. "$_omd_root/scripts/omd-path.sh"
unset _omd_root

exec "$OMD_ROOT/quickshell/scripts/quickshell" "$SUMIKA_MODULES_HOME/notification"
