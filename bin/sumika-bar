#!/bin/sh
set -eu

_sumika_root=$(cd "$(dirname "$0")/.." && pwd)
. "$_sumika_root/lib/paths.sh"
unset _sumika_root

exec "$SUMIKA_SHELL_ROOT/quickshell/scripts/quickshell" "$SUMIKA_SHELL_ROOT/apps/sumika-bar"
