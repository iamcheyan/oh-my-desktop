{
  "schemaVersion": 2,
  "id": "lock",
  "name": "Lock Screen",
  "description": "Screen lock with PAM authentication, fingerprint support, and session lock surfaces",
  "version": "2.0.0",
  "kind": "overlay",
  "contributes": {
    "overlays": [
      {"id": "lock", "name": "Lock Screen", "priority": 100, "component": "Lock.qml"}
    ],
    "actions": [
      {"id": "lock.lock", "name": "Lock screen", "description": "Lock the screen immediately"},
      {"id": "lock.unlock", "name": "Unlock screen", "description": "Unlock the screen"}
    ]
  }
}
