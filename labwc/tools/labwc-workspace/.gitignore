# Compiled binary — built by Init.sh (install_labwc_session) or `make`.
# Repo tracks source only (see AGENTS.md Git section).
labwc-workspace

# wayland-scanner generated protocol code (regenerated by make)
ext-ws-client.h
ext-ws-code.c
