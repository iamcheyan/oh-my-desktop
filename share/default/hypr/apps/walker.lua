-- Application-specific rules for Walker (running as_window).
o.window("dev.benz.walker", { float = true, center = true, animation = "none" })
hl.layer_rule({ match = { namespace = "walker" }, no_anim = true })
